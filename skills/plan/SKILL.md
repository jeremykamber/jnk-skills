---
name: jnk-5-plan
description: Merged into /skill:jnk-4-design — the shape and the route are one beat. This stub exists so stale invocations get a pointer instead of a dead skill.
disable-model-invocation: true
---

# Plan — merged into Design

`jnk-5-plan` no longer exists as its own beat. The shape and the route are one beat now: `/skill:jnk-4-design` — Phase 1 shapes the change (ASCII, contracts, failure paths), Phase 2 routes it (vertical slices, checkpoints, order, blast radius). The vertical-slice doctrine lives there. Invoke `/skill:jnk-4-design`.
