# jnk-7-debrief — notes

Private notes for the human. The agent never sees this file.

**What:** Close the session — the teach-back, the session log, what landed, squawks, IOUs reconciled, durable facts written home, the next step.

**When:** the session ends — always cheap, take it.

**Why:** a session leaves better software AND better understanding, or it is incomplete. The log names the next step; the loop only works if the memory is written.

**Fits:** closes the loop — jnk-0-pickup is the reader. Writes the worklog (notes.md) and durable facts (docs/external/).

**Refs:** the teach-back (if you can't explain it simply, you don't understand it).
